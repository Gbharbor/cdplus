#include <stdio.h>   // Necessário para funções de entrada e saída como printf e scanf
#include <string.h>  // Necessário para funções de manipulação de strings como strcpy, strcat, etc.

int main() {
    // O cabeçalho <string.h> fornece várias funções úteis para manipulação de strings em C.
    // Exemplos incluem:
    // - strcpy(): Copia uma string para outra.
    // - strcat(): Concatena duas strings.
    // - strlen(): Retorna o comprimento da string.
    // - strcmp(): Compara duas strings.
    // - strchr(): Encontra a primeira ocorrência de um caractere em uma string.
    // - strrchr(): Encontra a última ocorrência de um caractere em uma string.

    return 0; // Indica que o programa terminou corretamente
}
